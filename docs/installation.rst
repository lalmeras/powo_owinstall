.. highlight:: shell

============
Installation
============


Stable release
--------------

To install powo_owinstall, run this command in your terminal:

.. code-block:: console

    $ pip install powo_owinstall

This is the preferred method to install powo_owinstall, as it will always install the most recent stable release. 

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for powo_owinstall can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/lalmeras/powo_owinstall

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/lalmeras/powo_owinstall/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/lalmeras/powo_owinstall
.. _tarball: https://github.com/lalmeras/powo_owinstall/tarball/master
