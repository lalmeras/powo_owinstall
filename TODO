
* openssh >=7.0 skips dss / conf workaround PubkeyAcceptedKeyTypes +ssh-dss
* virtualbox modules needs recompilation
* fix javahl incompatilities (current eclipse build crash in fedora 24 - javahl 1.9.4)
